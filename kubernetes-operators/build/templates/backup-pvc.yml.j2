apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "backup-{{ name }}-pvc"
spec:
  storageClassName: "backup-{{ name }}-pv"
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: {{ storage_size }}
